name: Mobile Build (On-Demand)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Target platform'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - android
          - ios
      arch:
        description: 'Android architecture'
        required: false
        default: 'fat'
        type: choice
        options:
          - arm64
          - amd64
          - fat

env:
  GO_VERSION: "1.26.x"
  NODE_VERSION: "22"

jobs:
  # ==================== Android Build ====================
  android:
    if: inputs.platform == 'all' || inputs.platform == 'android' || github.event.inputs.platform == ''
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install bun
        run: npm install -g bun

      - name: Install Task & Wails CLI
        run: |
          go install github.com/go-task/task/v3/cmd/task@latest
          go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK r26d
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install "ndk;r26d" || \
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --install "ndk;r26d" || \
          echo "Warning: NDK r26d manual install failed, trying fallback..."
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/r26d" >> $GITHUB_ENV

      - name: Build arm64 APK
        if: inputs.arch == 'arm64' || inputs.arch == 'fat'
        run: task android:build ARCH=arm64

      - name: Build amd64 APK
        if: inputs.arch == 'amd64' || inputs.arch == 'fat'
        run: task android:build ARCH=amd64

      - name: Package fat APK
        if: inputs.arch == 'fat'
        run: task android:package:fat

      - name: Package single-arch APK
        if: inputs.arch != 'fat'
        run: task android:assemble:apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: swt-android-${{ inputs.arch || 'fat' }}
          path: bin/swt*.apk

  # ==================== iOS Build ====================
  ios:
    if: inputs.platform == 'all' || inputs.platform == 'ios' || github.event.inputs.platform == ''
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install bun
        run: npm install -g bun

      - name: Install Task & Wails CLI
        run: |
          go install github.com/go-task/task/v3/cmd/task@latest
          go install github.com/wailsapp/wails/v3/cmd/wails3@latest

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest

      - name: Build iOS App
        run: task ios:package

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: swt-ios
          path: bin/swt.app
